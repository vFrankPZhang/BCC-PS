# 章节及其目标和前后逻辑

## 章

BCCA-PS
1.PowerShell概述  
2.开始使用PowerShell  
3.认识PowerShell的命令  
4.使用PowerShell的帮助系统  
5.管道的使用  
6.对象  
7.对象的操作  
8.格式化和输出  
9.变量和输入  
10.文件系统  
11.远程处理  
12.使用WMI  
13.作业Job  
14.扩展命令  
15.使用脚本  

## 校对需求说明

以第一章为例，下面的详细内容包含3个部分：

1.章的标题  
2.该章的目标和章节逻辑  
3.该章的每一节的标题  
4.每一节下面是该节大概讲什么  

1和3是用于给读者看的，是章和节的标题，所以校对者需要帮忙看一下，这个章和节的名字取的是否合适
2和4是用于给参与创作的人看的，不是给读者看的，用于描述节里面是什么内容，以及通过这些节能否实现章的目标

### 目标和章节逻辑

让读者对PowerShell有一个初步的认知，知道PowerShell是什么，能干什么，有哪些重要的特性，比如基于对象。

## 1.PowerShell概述

### 目标和章节逻辑

让读者对PowerShell有一个初步的认知，知道PowerShell是什么，能干什么，有哪些重要的特性，比如基于对象。

### 节

* 1.1什么是PowerShell  
是一种命令行管理工具，还是一种脚本语言
* 1.2PowerShell的重要特性  
基于.NET Framework  
基于对象  
命令可扩展
PowerShell使用一些c#的语法
拥抱开源

## 2.开始使用PowerShell

### 目标和章节逻辑

介绍完PS是什么后，让读者准备好PowerShell的使用环境，知道从哪打开PowerShell控制台，然后跟随教程运行一条PowerShell命令，体验一下如何通过PowerShell完成工作

### 节

* 2.1安装PowerShell  
Windows不需要安装，我们使用Windows 10系统，PowerShell 5.1版本  
Linux安装PowerShell  
MAC安装PowerShell

* 2.2打开PowerShell控制台  
打开PowerShell控制台  
查看当前PowerShell版本
* 2.3执行一条Powershell命令  
Get-Process -Name notepad来体验一下如果通过powershell查看记事本应用的进程信息

## 3.认识PowerShell的命令

### 目标和章节逻辑

基于上一章对PS命令的第一次使用，通过本章来说明PowerShell的命令是如何组成的，如：一条命令包括命令，参数，参数值，以此进一步介绍命令是由动词+名词组成，理解这种命令的结构的意义和好处，如：可以通过命令中的动词猜到该命令能做什么，可以通过命令中的名词猜到该命令执行目标是谁，进而基于上面的对命令结构的知识，学会通过get-command找到需要的命令，最后介绍PS的别名机制，来提升命令使用的效率以及理解PS通过别名的方式来集成其他环境使用命令行工作的经验，如：linux中ls查看目录中的文件，在PS中也可以通过执行ls来查看目录中的文件。以此来最大程度的降低使用PS的过渡成本。

### 节

* 3.1PowerShell命令的结构  
cmdlet使用动词-名词的命名规则  
cmdlet使用标准的参数名，比如-Name，在很多命令中都存在，且意义相同.
* 3.2获取需要的PowerShell命令  
基于对动词-名词的结构的理解，通过Get-Command命令来找到需要的命令
* 3.3PowerShell命令的别名  
查看别名  
PS定义的标准别名有哪些  
创建新的别名

## 4.使用PowerShell的帮助系统

### 目标和章节逻辑

前面学习了怎么找命令，接下来就该学习如何使用命令了，所以，接下来是学会使用PowerShell的帮助系统，来了解怎么使用一个命令，但是帮助系统分为了命令的帮助和参数的帮助信息，因为参数的帮助信息里面的内容需要依赖太多后面的知识，所以将知识范围限定在命令的帮助里，比如一个命令有哪些参数？哪些参数集？可选必选不讲，前期就要求使用命令必须提供参数名，如何查看命令的例子、online打开等知识。

### 节

* 4.1获得命令的帮助信息
* 4.2参数和参数集
* 4.3查看帮助示例
* 4.4更新本地帮助信息和使用在线帮助信息

## 5.管道的使用

### 目标和章节逻辑

通过一条命令获取记事本的进程并停止记事本的进程来引出管道的基本使用，掌握通过管道连接两个命令的使用方式，为通过get-member查看对象内容，对象的操作做准备，不讲对象在管道中的传递，等学完对象的操作，理解了对象的属性，学习了参数的帮助信息后，讲解管道的深入理解。

### 节

* 5.1 连接一个命令与另一个命令

以Get-Process -Name notepad | Stop-Process为例，引出如何通过管道，将两条命令连接起来，以实现一条命令解决问题。

* 5.2 输出结果到CSV或XML文件
关于输出会有具体的章节来讲解，这里主要是通过将结果输出来掌握管道对命令的连接的使用。

## 6.对象

### 目标和章节逻辑

通过PowerShell命令的输出结果，引出对象，比如process是一种对象，我们可以理解为是一个物体，而物体有很多属性，比如人有性别，年龄，肤色等等属性，而process也有各种属性，通过Get-Process，可以看到进程的一部分属性，通过get-process | Get-Member可以查看对象的所有属性，其实，我们看到，对象不只包含属性，还包含两个信息，一个是对象的类型，还有对象有哪些方法，方法可以理解为能对对象干什么，通过上面的内容，来建立读者对对象的一个认识，建立对象与生活中事物的类比关系，理解对象。

### 节

* 6.1 什么是对象
对象包含对象的类型、属性和方法，举例子来描述这些东西是什么
* 6.2 探索对象
讲解通过Get-Member来看，对象的这些东西是什么样的
* 6.3 对象的属性
具体看一下对象的属性
* 6.4 对象的方法
具体看一下对象的方法

## 7.对象的操作

### 目标和章节逻辑

理解了对象的属性、方法、类型，就可以开始操作对象，先学习基于属性的各种对象的操作，然后学习通过方法操作对象

### 节

* 7.1 通过属性操作对象
select/where/sort/group/measure/compare/foreach
* 7.2 通过方法操作对象
* 7.3 对象在管道中的传递
介绍管道的工作机制，理解对象通过管道基于ByPropertyName和ByValue被传入参数中。

## 8.格式化和输出

### 目标和章节逻辑

学习格式化命令，来掌握对powershell在命令行中输出结果的显示样式的控制，以便更好的阅读和导出输出结果

### 节

* 8.1 格式化表格Format-Table
介绍FT的效果，FT中，-autosize,-property,-groupby,-wrap等参数的作用。
* 8.2 格式化列表Format-List
介绍FL的效果
* 8.3 格式化宽列表
介绍FW的效果
* 8.4 创建自定义列表与条目
介绍如何通过哈希表创建自定义的列表和列表显示条目
* 8.5 默认格式
介绍PS如何定义的默认格式，比如Get-Service的命令输出结果默认使用FT还是FL来呈现，如何定义的。

## 9.变量和输入

### 目标和章节逻辑

学习什么是变量，如何将内容输入到powershell并保存到变量里面，比如把一组机器名称导入powershell并保存到变量里面，之后能同时获取变量里面的机器的信息，这部分知识是为能够对多个目标同时操作做准备。

### 节

* 9.1 什么是变量
内存中一个带有名字的可以存放东西的盒子。
* 9.2 把数据存入变量
如何把数据赋值给变量，把单个值赋值给变量，把多个值赋值给变量
* 9.3 使用变量
使用单值变量中的值
引号的用途
使用多值变量中的单个值
使用多值变量中的多个值
与多个变量交互
    ForEach-Object
* 9.4 变量的类型
前面先把变量用起来，然后学习其实变量是分各种类型的，分别叫什么，不同的变量类型装不同类型的值
* 9.5 声明变量类型

## 10.文件系统

### 目标和章节逻辑

学习如何通过PowerShell操作windows文件系统，并通过一个复杂的文件系统使用需求，将对象的操作，管道，变量，输入的知识串联到一起。

### 节

## 11.远程处理

### 目标和章节逻辑

掌握了前面的知识，就可以进行本地的PowerShell的各种使用了，接下来，是如何远程来执行PowerShell解决1对1和1对多的远程处理需求。

### 节

* 11.1 一对一的远程处理
* 11.2 一对多的远程处理
* 11.3 本地处理和远程处理对比

## 12.使用WMI

### 目标和章节逻辑

学习使用WMI，在扩展通过PowerShell能做的事情。

### 节

* 12.1 WMI概要
* 12.2 在PowerShell中使用WMI
    get-wmiobject和get-ciminstance
* 12.3 如何找到需要使用的WMI

## 13.作业Job

### 目标和章节逻辑

通过学习Job，来提升PowerShell使用的效率

### 节

* 13.1 利用PowerShell实现多任务同时处理
* 13.2 任务处理的同步和异步
* 13.3 创建本地Job
* 13.4 创建WMI Job
* 13.5 远程处理Job
* 13.6 获取Job的处理结果
* 13.7 使用子Job
* 13.8 Job的管理
* 13.9 Schedule Job

## 14.扩展命令

### 目标和章节逻辑

通过学习snapin和module的相关知识来扩展powershell可使用的命令。

### 节

* 14.1 只有一个PowerShell
无论什么产品的PowerShell其实都只是一个PowerShell，只是加载的不同的扩展命令集
* 14.2 PSSnapin
* 14.3 PSModule

## 15.使用脚本

### 目标和章节逻辑

学习如何在PowerShell中使用脚本来工作。

### 节
